<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="20dp"
        android:paddingTop="28dp"
        android:gravity="center_vertical"
        android:background="@drawable/bg_hero_gradient">

        <ImageView
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/logo_badge"
            android:layout_marginEnd="12dp"
            android:contentDescription="@null" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Admin Dashboard"
            style="@style/Text.Heading"
            android:textSize="20sp" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <include layout="@layout/item_dashboard_menu" android:id="@+id/menuEnrollments" />
            <include layout="@layout/item_dashboard_menu" android:id="@+id/menuResources" />
            <include layout="@layout/item_dashboard_menu" android:id="@+id/menuTimetables" />
            <include layout="@layout/item_dashboard_menu" android:id="@+id/menuReviews" />
            <include layout="@layout/item_dashboard_menu" android:id="@+id/menuSettings" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnLogout"
                style="@style/Widget.Francirene.Button.Ghost"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="Log Out" />

        </LinearLayout>
    </ScrollView>

</LinearLayout>
